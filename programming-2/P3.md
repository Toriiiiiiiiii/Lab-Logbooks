# Lab P3
## Logbook

1. A HashSet is a variant of the Set class which stores an unordered collection of data with the same type. Unlike most other collections, there is no specific order to the data stored in a Set. The hash set specifically uses a hash map for its implementation, instead of an array or linked list.
2. I iterated through each word in the groups using `getWordArray()`, and then using `wordSet.add(word)` to add each word to the set. As the set will automatically handle duplicates, there was no need for any extra logic or conditions.
3. From the Java API Reference, I learned that the Set class has a `.add()` method to quickly add new elements to the set.
4. Referring to the API Reference is extremely useful, as it allows you to learn how a class functions and provides a concise overview of all the methods, quickly describing the ways the class can be used.
